-- ============================================================
-- RST CRM: コール履歴にアポ日時（appo_at）を保持
-- コール結果がアポの場合、そのコール履歴カードにアポ時間を表示するため。
-- 冪等。npm run db:apply で適用。
-- ============================================================
ALTER TABLE call_logs ADD COLUMN IF NOT EXISTS appo_at TIMESTAMPTZ;
