-- ============================================================
-- アポの形式（Zoom / 対面）。訪問予定の所要時間の既定値に使う。
--   対面 = 2時間枠 / zoom = 1時間枠
-- 既存データは対面扱い（従来の2時間枠と同じ挙動）にするため NULL のままでよい。
-- 冪等: IF NOT EXISTS。
-- ============================================================
ALTER TABLE appointments ADD COLUMN IF NOT EXISTS meeting_type TEXT;  -- 'zoom' | '対面'
