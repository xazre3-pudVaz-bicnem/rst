-- ============================================================
-- 成約案件（訪問結果）の営業担当。
--   ユーザー登録済みの担当名（織田春樹・織田哲哉 等）または「販売代理店」を保存する。
-- 既存データは NULL（未設定）のまま。冪等: IF NOT EXISTS。
-- ============================================================
ALTER TABLE visit_reports ADD COLUMN IF NOT EXISTS sales_rep TEXT;
